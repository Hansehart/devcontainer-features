{
  "id": "codex",
  "version": "1.0.1",
  "name": "Codex CLI",
  "description": "Installs the OpenAI Codex CLI on a selectable channel.",
  "documentationURL": "https://github.com/hansehart/devcontainer-features/tree/main/src/codex",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Channel (latest) or an exact version (major.minor.patch)."
    },
    "stateDir": {
      "type": "string",
      "default": "",
      "description": "If set, puts this feature's data at this path. Mount a volume there to persist it across rebuilds."
    },
    "configToml": {
      "type": "string",
      "default": "",
      "description": "Codex config as TOML with backslash-escaped quotes and line breaks, written to config.toml."
    }
  },
  "postCreateCommand": "/usr/local/share/codex/init.sh"
}

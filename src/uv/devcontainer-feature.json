{
  "id": "uv",
  "version": "1.1.6",
  "name": "uv (Python)",
  "description": "Installs the uv Python package and interpreter manager as a single static binary.",
  "documentationURL": "https://github.com/hansehart/devcontainer-features/tree/main/src/uv",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Channel (latest) or an exact version (major.minor.patch)."
    },
    "python": {
      "type": "string",
      "proposals": ["", "latest"],
      "default": "",
      "description": "Installs this CPython at build as the default (adds python/python3 to PATH). Empty installs no interpreter, leaving uv to use an available Python or download one on demand."
    },
    "stateDir": {
      "type": "string",
      "default": "",
      "description": "If set, puts this feature's data at this path. Mount a volume there to persist it across rebuilds."
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "charliermarsh.ruff",
        "ms-python.mypy-type-checker",
        "ms-python.python"
      ]
    }
  },
  "postCreateCommand": "/usr/local/share/uv/init.sh"
}

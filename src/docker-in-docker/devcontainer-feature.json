{
  "id": "docker-in-docker",
  "version": "1.0.1",
  "name": "Docker in Docker",
  "description": "Installs a Docker engine for building and running containers inside the dev container.",
  "documentationURL": "https://github.com/hansehart/devcontainer-features/tree/main/src/docker-in-docker",
  "options": {
    "version": {
      "type": "string",
      "proposals": ["latest"],
      "default": "latest",
      "description": "Channel (latest) or an exact version (major.minor.patch)."
    },
    "daemonJson": {
      "type": "string",
      "default": "",
      "description": "Daemon settings as a JSON object with backslash-escaped quotes, merged into daemon.json."
    }
  },
  "entrypoint": "/usr/local/share/docker-init.sh",
  "privileged": true,
  "containerEnv": {
    "DOCKER_BUILDKIT": "1"
  },
  "mounts": [
    {
      "source": "dind-var-lib-docker",
      "target": "/var/lib/docker",
      "type": "volume"
    },
    {
      "source": "dind-var-lib-containerd",
      "target": "/var/lib/containerd",
      "type": "volume"
    }
  ]
}
